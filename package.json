{
  "name": "line-drive-archiver",
  "version": "1.0.0",
  "description": "LINE chatbot that archives everything you send it (links, photos, videos, files) into Google Drive",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "get-token": "node scripts/get-refresh-token.js",
    "check": "node --check src/index.js && node --check src/drive.js && node --check scripts/get-refresh-token.js"
  },
  "dependencies": {
    "@line/bot-sdk": "^9.5.0",
    "express": "^4.21.2",
    "googleapis": "^144.0.0"
  },
  "license": "MIT"
}
